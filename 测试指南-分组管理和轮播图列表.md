# 测试指南 - 分组管理和轮播图列表功能

## 📋 测试环境准备

### 1. 启动后端服务
```bash
cd h:\cscmswy\Logistics-website-backend
mvn spring-boot:run
```

**验证后端启动成功**:
- 访问: `http://localhost:8080/api`
- 确认服务正常运行

### 2. 启动前端服务
```bash
cd h:\cscmswy\Logistics-website
npm run dev
```

**验证前端启动成功**:
- 访问: `http://localhost:5173`
- 确认页面正常加载

### 3. 登录管理后台
- 访问: `http://localhost:5173/admin`
- 使用管理员账号登录
- 进入"轮播图管理"页面

---

## 🧪 功能测试用例

### 测试用例 1: 分组筛选显示所有分组（包括禁用的）

**测试步骤**:
1. 进入"轮播图管理"页面
2. 点击"管理分组"按钮
3. 禁用一个分组（选择"仅禁用分组"模式）
4. 关闭分组管理对话框
5. 查看分组筛选下拉框

**预期结果**:
- ✅ 下拉框中显示所有分组（包括刚才禁用的分组）
- ✅ 禁用的分组后面显示红色"禁"字标识
- ✅ 可以选择禁用的分组进行筛选
- ✅ 选择禁用的分组后，列表显示该分组下的所有轮播图

**测试数据**:
```
分组列表示例:
- 首页 (home)
- 关于我们 (about)
- 产品中心 (product) [禁]
- 新闻中心 (news)
```

---

### 测试用例 2: 新增轮播图时禁用的分组不可选

**测试步骤**:
1. 进入"轮播图管理"页面
2. 点击"管理分组"按钮
3. 禁用一个分组（任意模式）
4. 关闭分组管理对话框
5. 切换到"轮播图新增"标签页
6. 点击"分组"下拉框

**预期结果**:
- ✅ 下拉框中显示所有分组
- ✅ 禁用的分组显示为灰色
- ✅ 禁用的分组后面显示"(已禁用)"文字
- ✅ 禁用的分组无法选择（点击无反应）
- ✅ 只能选择启用的分组

**测试数据**:
```
分组下拉框示例:
- 首页
- 关于我们
- 产品中心 (已禁用) [灰色，不可选]
- 新闻中心
```

---

### 测试用例 3: 修改轮播图时禁用的分组不可选

**测试步骤**:
1. 进入"轮播图管理"页面
2. 点击"管理分组"按钮
3. 禁用一个分组（任意模式）
4. 关闭分组管理对话框
5. 在轮播图列表中点击某个轮播图的"修改"按钮
6. 查看"分组"下拉框

**预期结果**:
- ✅ 下拉框中显示所有分组
- ✅ 禁用的分组显示为灰色
- ✅ 禁用的分组后面显示"(已禁用)"文字
- ✅ 禁用的分组无法选择
- ✅ 如果当前轮播图属于禁用的分组，仍然显示该分组名称，但无法修改为其他禁用的分组

---

### 测试用例 4: 禁用分组 - 模式A（仅禁用分组）

**测试步骤**:
1. 进入"轮播图管理"页面
2. 点击"管理分组"按钮
3. 选择一个有轮播图的分组（例如"产品中心"）
4. 记录该分组下的轮播图数量和状态
5. 点击该分组的状态开关（从启用切换到禁用）
6. 在弹出的确认对话框中选择"仅禁用分组"
7. 点击"确定"按钮

**预期结果**:
- ✅ 显示确认对话框，标题为"禁用分组确认"
- ✅ 对话框中显示分组名称
- ✅ 默认选中"仅禁用分组"选项
- ✅ 点击确定后，分组状态变为禁用
- ✅ 分组列表中该分组显示红色"禁"字标识
- ✅ 该分组下的轮播图状态不变（仍然是启用状态）
- ✅ 显示成功消息："分组已禁用，该分组下的轮播图不受影响"
- ✅ 前端页面仍然显示该分组下的轮播图

**测试数据**:
```
测试前:
- 分组: 产品中心 (product)
- 状态: 启用
- 轮播图数量: 3张
- 轮播图状态: 全部启用

测试后:
- 分组: 产品中心 (product) [禁]
- 状态: 禁用
- 轮播图数量: 3张
- 轮播图状态: 全部启用（不变）
```

---

### 测试用例 5: 禁用分组 - 模式B（禁用分组并关闭所有轮播图）

**测试步骤**:
1. 进入"轮播图管理"页面
2. 点击"管理分组"按钮
3. 选择一个有轮播图的分组（例如"新闻中心"）
4. 记录该分组下的轮播图数量和状态
5. 点击该分组的状态开关（从启用切换到禁用）
6. 在弹出的确认对话框中选择"禁用分组并关闭所有轮播图"
7. 点击"确定"按钮

**预期结果**:
- ✅ 显示确认对话框，标题为"禁用分组确认"
- ✅ 对话框中显示分组名称
- ✅ 可以选择"禁用分组并关闭所有轮播图"选项
- ✅ 点击确定后，分组状态变为禁用
- ✅ 分组列表中该分组显示红色"禁"字标识
- ✅ 该分组下的所有轮播图状态变为禁用
- ✅ 显示成功消息："分组已禁用，该分组下的所有轮播图也已禁用"
- ✅ 前端页面不再显示该分组下的轮播图
- ✅ 在轮播图列表中筛选该分组，可以看到所有轮播图状态都是禁用

**测试数据**:
```
测试前:
- 分组: 新闻中心 (news)
- 状态: 启用
- 轮播图数量: 5张
- 轮播图状态: 全部启用

测试后:
- 分组: 新闻中心 (news) [禁]
- 状态: 禁用
- 轮播图数量: 5张
- 轮播图状态: 全部禁用
```

---

### 测试用例 6: 启用已禁用的分组

**测试步骤**:
1. 进入"轮播图管理"页面
2. 点击"管理分组"按钮
3. 找到一个已禁用的分组
4. 点击该分组的状态开关（从禁用切换到启用）

**预期结果**:
- ✅ 不显示确认对话框（启用操作直接执行）
- ✅ 分组状态立即变为启用
- ✅ 分组列表中该分组的"禁"字标识消失
- ✅ 显示成功消息："状态更新成功"
- ✅ 新增/修改轮播图时可以选择该分组
- ✅ 注意：启用分组不会自动启用该分组下的轮播图（需要手动启用）

---

### 测试用例 7: 取消禁用分组操作

**测试步骤**:
1. 进入"轮播图管理"页面
2. 点击"管理分组"按钮
3. 选择一个启用的分组
4. 点击该分组的状态开关（从启用切换到禁用）
5. 在弹出的确认对话框中点击"取消"按钮

**预期结果**:
- ✅ 对话框关闭
- ✅ 分组状态保持启用（不变）
- ✅ 分组列表中该分组没有"禁"字标识
- ✅ 不显示任何消息

---

### 测试用例 8: 轮播图列表中显示分组禁用状态

**测试步骤**:
1. 进入"轮播图管理"页面
2. 点击"管理分组"按钮
3. 禁用一个有轮播图的分组（选择"仅禁用分组"模式）
4. 关闭分组管理对话框
5. 在轮播图列表中查看该分组下的轮播图

**预期结果**:
- ✅ 轮播图列表中显示该分组的轮播图
- ✅ 分组列中显示分组名称
- ✅ 分组名称后面显示红色"禁"字标识
- ✅ 轮播图的状态开关仍然可以正常使用

**测试数据**:
```
轮播图列表示例:
序号 | 分组          | 标题        | 状态
-----|--------------|-------------|------
1    | 首页         | 首页轮播1   | 启用
2    | 产品中心 [禁] | 产品轮播1   | 启用
3    | 产品中心 [禁] | 产品轮播2   | 启用
4    | 新闻中心     | 新闻轮播1   | 启用
```

---

### 测试用例 9: 分组筛选功能

**测试步骤**:
1. 进入"轮播图管理"页面
2. 点击分组筛选下拉框
3. 选择一个禁用的分组
4. 查看轮播图列表

**预期结果**:
- ✅ 列表只显示该分组下的轮播图
- ✅ 可以看到该分组下的所有轮播图（包括禁用的）
- ✅ 分组列显示红色"禁"字标识
- ✅ 可以对这些轮播图进行修改、删除、状态切换等操作

---

### 测试用例 10: 批量禁用轮播图后的数据一致性

**测试步骤**:
1. 进入"轮播图管理"页面
2. 点击"管理分组"按钮
3. 选择一个有5张轮播图的分组
4. 禁用该分组，选择"禁用分组并关闭所有轮播图"
5. 关闭分组管理对话框
6. 在分组筛选中选择该分组
7. 查看轮播图列表

**预期结果**:
- ✅ 列表显示该分组下的5张轮播图
- ✅ 所有轮播图的状态开关都是关闭状态（禁用）
- ✅ 分组列显示红色"禁"字标识
- ✅ 可以手动启用单个轮播图
- ✅ 前端页面不显示这些轮播图（因为都是禁用状态）

---

## 🔍 边界条件测试

### 边界测试 1: 禁用没有轮播图的分组

**测试步骤**:
1. 创建一个新分组（不添加任何轮播图）
2. 禁用该分组，选择"禁用分组并关闭所有轮播图"

**预期结果**:
- ✅ 分组成功禁用
- ✅ 显示消息："成功禁用 0 张轮播图"
- ✅ 不报错

---

### 边界测试 2: 禁用已经全部禁用轮播图的分组

**测试步骤**:
1. 选择一个分组，手动禁用该分组下的所有轮播图
2. 禁用该分组，选择"禁用分组并关闭所有轮播图"

**预期结果**:
- ✅ 分组成功禁用
- ✅ 显示消息："分组已禁用，该分组下的所有轮播图也已禁用"
- ✅ 轮播图状态保持禁用（不变）
- ✅ 不报错

---

### 边界测试 3: 快速切换分组状态

**测试步骤**:
1. 禁用一个分组（选择"仅禁用分组"）
2. 立即启用该分组
3. 再次禁用该分组（选择"禁用分组并关闭所有轮播图"）
4. 再次启用该分组

**预期结果**:
- ✅ 每次操作都成功执行
- ✅ 状态正确更新
- ✅ 不出现状态不一致的情况
- ✅ 不报错

---

### 边界测试 4: 修改属于禁用分组的轮播图

**测试步骤**:
1. 禁用一个分组（选择"仅禁用分组"）
2. 在轮播图列表中找到该分组下的一个轮播图
3. 点击"修改"按钮
4. 尝试修改轮播图的标题、图片等信息
5. 保存修改

**预期结果**:
- ✅ 可以正常修改轮播图信息
- ✅ 分组下拉框中该分组显示为禁用状态
- ✅ 无法将轮播图移动到其他禁用的分组
- ✅ 可以将轮播图移动到启用的分组
- ✅ 修改成功保存

---

## 🐛 异常情况测试

### 异常测试 1: 网络错误时的处理

**测试步骤**:
1. 停止后端服务
2. 尝试禁用一个分组

**预期结果**:
- ✅ 显示错误消息："禁用失败：网络错误"
- ✅ 分组状态恢复到原来的状态（启用）
- ✅ 不出现白屏或崩溃

---

### 异常测试 2: 后端返回错误时的处理

**测试步骤**:
1. 模拟后端返回错误（可以通过修改后端代码或使用代理工具）
2. 尝试禁用一个分组

**预期结果**:
- ✅ 显示错误消息："禁用失败：错误信息"
- ✅ 分组状态恢复到原来的状态
- ✅ 不出现白屏或崩溃

---

### 异常测试 3: 并发操作测试

**测试步骤**:
1. 打开两个浏览器标签页，都进入轮播图管理页面
2. 在第一个标签页中禁用一个分组
3. 在第二个标签页中尝试修改该分组下的轮播图

**预期结果**:
- ✅ 第一个标签页成功禁用分组
- ✅ 第二个标签页可以修改轮播图（因为数据还没刷新）
- ✅ 刷新第二个标签页后，分组显示为禁用状态

---

## 📊 性能测试

### 性能测试 1: 大量轮播图的批量禁用

**测试步骤**:
1. 创建一个分组，添加100张轮播图
2. 禁用该分组，选择"禁用分组并关闭所有轮播图"
3. 记录操作时间

**预期结果**:
- ✅ 操作在合理时间内完成（建议 < 5秒）
- ✅ 所有轮播图状态正确更新
- ✅ 不出现超时错误

---

### 性能测试 2: 大量分组的筛选性能

**测试步骤**:
1. 创建50个分组
2. 禁用其中20个分组
3. 打开分组筛选下拉框
4. 滚动查看所有分组

**预期结果**:
- ✅ 下拉框流畅显示
- ✅ 禁用的分组正确显示"禁"字标识
- ✅ 选择分组后列表快速更新

---

## ✅ 测试检查清单

### 功能完整性
- [ ] 分组筛选显示所有分组（包括禁用的）
- [ ] 禁用的分组显示"禁"字标识
- [ ] 新增轮播图时禁用的分组不可选
- [ ] 修改轮播图时禁用的分组不可选
- [ ] 禁用分组时显示确认对话框
- [ ] 两种禁用模式都正常工作
- [ ] 启用分组直接执行，不显示对话框
- [ ] 轮播图列表中显示分组禁用状态

### 用户体验
- [ ] 操作流程顺畅，无卡顿
- [ ] 错误提示清晰明确
- [ ] 成功消息及时显示
- [ ] 对话框文字描述清楚
- [ ] 视觉标识明显（"禁"字、灰色、不可选）

### 数据一致性
- [ ] 禁用分组后数据正确更新
- [ ] 批量禁用轮播图后状态一致
- [ ] 启用分组后状态正确
- [ ] 前端显示与后端数据一致

### 异常处理
- [ ] 网络错误时有友好提示
- [ ] 后端错误时有友好提示
- [ ] 状态恢复机制正常工作
- [ ] 不出现白屏或崩溃

---

## 🎯 测试报告模板

### 测试环境
- 操作系统: Windows 11
- 浏览器: Chrome 120
- 后端版本: Spring Boot 3.2.5
- 前端版本: Vue 3 + Element Plus

### 测试结果汇总
| 测试用例 | 测试结果 | 备注 |
|---------|---------|------|
| 测试用例 1 | ✅ 通过 | |
| 测试用例 2 | ✅ 通过 | |
| 测试用例 3 | ✅ 通过 | |
| 测试用例 4 | ✅ 通过 | |
| 测试用例 5 | ✅ 通过 | |
| 测试用例 6 | ✅ 通过 | |
| 测试用例 7 | ✅ 通过 | |
| 测试用例 8 | ✅ 通过 | |
| 测试用例 9 | ✅ 通过 | |
| 测试用例 10 | ✅ 通过 | |

### 发现的问题
1. 无

### 测试结论
所有功能测试通过，系统运行稳定，满足需求文档要求。

---

## 📞 问题反馈

如果在测试过程中发现任何问题，请记录以下信息：

1. **问题描述**: 详细描述问题现象
2. **复现步骤**: 列出导致问题的操作步骤
3. **预期结果**: 说明应该出现的正确结果
4. **实际结果**: 说明实际出现的错误结果
5. **截图**: 如果可能，提供问题截图
6. **浏览器控制台**: 提供浏览器控制台的错误信息
7. **后端日志**: 提供后端服务的错误日志

---

## 🚀 快速测试脚本

### 使用 curl 测试后端接口

```bash
# 1. 获取所有分组
curl -H "X-Tenant-Id: 1" http://localhost:8080/api/tenant/carousel-group/list

# 2. 获取所有轮播图
curl -H "X-Tenant-Id: 1" http://localhost:8080/api/tenant/carousel/list

# 3. 批量禁用分组下的轮播图
curl -X PUT -H "X-Tenant-Id: 1" http://localhost:8080/api/tenant/carousel/group/product/disable

# 4. 禁用分组
curl -X PUT \
  -H "X-Tenant-Id: 1" \
  -H "Content-Type: application/json" \
  -d '{
    "groupKey": "product",
    "groupName": "产品中心",
    "description": "产品展示页面",
    "sortOrder": 3,
    "isEnabled": 0
  }' \
  http://localhost:8080/api/tenant/carousel-group/1

# 5. 启用分组
curl -X PUT \
  -H "X-Tenant-Id: 1" \
  -H "Content-Type: application/json" \
  -d '{
    "groupKey": "product",
    "groupName": "产品中心",
    "description": "产品展示页面",
    "sortOrder": 3,
    "isEnabled": 1
  }' \
  http://localhost:8080/api/tenant/carousel-group/1
```

---

## 📝 测试注意事项

1. **测试数据准备**: 确保数据库中有足够的测试数据（至少3个分组，每个分组至少3张轮播图）
2. **测试顺序**: 建议按照测试用例编号顺序执行测试
3. **数据恢复**: 每个测试用例执行完后，建议恢复数据到初始状态
4. **浏览器缓存**: 测试前清除浏览器缓存，避免缓存影响测试结果
5. **并发测试**: 并发测试时使用不同的浏览器或无痕模式
6. **日志记录**: 测试过程中注意查看浏览器控制台和后端日志

---

祝测试顺利！🎉
