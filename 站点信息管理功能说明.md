# 站点信息管理功能说明

## 功能概述
已完成站点信息管理的前端开发，管理员可以通过后台管理系统配置站点的基本信息。

## 已完成的文件

### 1. API接口文件
**文件路径**: `src/api/siteInfo.js`

包含以下接口：
- `getSiteInfo()` - 获取站点信息
- `updateSiteInfo(data)` - 更新站点信息
- `uploadFile(file)` - 上传文件（LOGO等）

### 2. 管理页面组件
**文件路径**: `src/pages/admin/SiteInfoManagement.vue`

功能特性：
- 站点标题配置（必填，最多200字符）
- 站点副标题配置（最多200字符）
- 站点域名配置（带域名格式验证）
- 站点LOGO上传（支持图片预览和删除）
- 尾部LOGO上传（支持图片预览和删除）
- 站点备案号配置（最多100字符）
- 站点模板选择（默认/简约/商务）
- 尾部信息配置（最多500字符）
- 表单验证和错误提示
- 保存和重置功能

### 3. 路由配置
**文件路径**: `src/router/index.js`

新增路由：
```javascript
{
  path: 'site-info',
  name: 'SiteInfoManagement',
  component: () => import('../pages/admin/SiteInfoManagement.vue'),
  meta: { title: '站点信息管理 - 后台管理', requiresAuth: true }
}
```

### 4. 菜单配置
**文件路径**: `src/layouts/AdminLayout.vue`

更新内容：
- 在"基本信息"菜单组下新增"站点信息"菜单项
- 菜单项位于"导航栏目"之前
- 支持双击刷新功能
- 面包屑导航显示"站点信息管理"

### 5. 工具类更新
**文件路径**: `src/utils/request.js`

更新内容：
- 导出 `BASE_URL` 常量供其他模块使用
- 统一管理API基础地址

## 数据库字段对应关系

根据 `初始化数据库结构设计.sql` 中的 `site_info` 表结构：

| 表单字段 | 数据库字段 | 说明 |
|---------|-----------|------|
| 站点标题 | site_title | VARCHAR(200) |
| 站点副标题 | site_subtitle | VARCHAR(200) |
| 站点域名 | site_domain | VARCHAR(255) |
| 站点LOGO | site_logo | VARCHAR(500) |
| 尾部LOGO | footer_logo | VARCHAR(500) |
| 站点备案号 | site_icp | VARCHAR(100) |
| 站点模板 | site_template | VARCHAR(100) |
| 尾部信息 | footer_info | TEXT |

## 访问路径

- 后台管理入口：`http://localhost:5173/admin`
- 站点信息管理：`http://localhost:5173/admin/site-info`

## 后端API接口要求

需要后端实现以下接口：

### 1. 获取站点信息
```
GET /api/tenant/site-info
Headers: X-Tenant-Id: {租户ID}
Response: {
  code: 200,
  data: {
    siteTitle: string,
    siteSubtitle: string,
    siteDomain: string,
    siteLogo: string,
    footerLogo: string,
    siteIcp: string,
    siteTemplate: string,
    footerInfo: string
  }
}
```

### 2. 更新站点信息
```
PUT /api/tenant/site-info
Headers: X-Tenant-Id: {租户ID}
Body: {
  siteTitle: string,
  siteSubtitle: string,
  siteDomain: string,
  siteLogo: string,
  footerLogo: string,
  siteIcp: string,
  siteTemplate: string,
  footerInfo: string
}
Response: {
  code: 200,
  message: "更新成功"
}
```

### 3. 文件上传
```
POST /api/tenant/upload
Headers:
  X-Tenant-Id: {租户ID}
  Content-Type: multipart/form-data
Body: FormData with 'file' field
Response: {
  code: 200,
  data: {
    url: string  // 相对路径，如 "/uploads/logo.png"
  }
}
```

## 注意事项

1. **图片上传限制**：
   - 只支持图片格式（image/*）
   - 文件大小不超过 2MB
   - 建议LOGO尺寸：200x60px

2. **域名验证**：
   - 必须符合标准域名格式
   - 例如：example.com, subdomain.example.com

3. **租户隔离**：
   - 所有请求都会自动添加 `X-Tenant-Id` 请求头
   - 租户ID从 localStorage 获取，默认为 "1"

4. **图片URL处理**：
   - 支持相对路径和绝对路径
   - 相对路径会自动拼接 BASE_URL

## 下一步工作

后端需要实现：
1. 站点信息的增删改查接口
2. 文件上传接口（支持图片上传到服务器）
3. 租户隔离逻辑（根据 X-Tenant-Id 过滤数据）
4. 数据验证和错误处理

## 测试建议

1. 测试表单验证（必填项、字符长度、域名格式）
2. 测试图片上传功能（格式限制、大小限制）
3. 测试图片预览和删除功能
4. 测试保存和重置功能
5. 测试菜单导航和双击刷新功能
